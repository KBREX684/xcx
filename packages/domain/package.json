{
  "name": "@agent-control-plane/domain",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src/**/*.test.ts",
    "verify": "tsx bin/acp-verify.ts"
  },
  "bin": {
    "acp-verify": "./bin/acp-verify.ts"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0"
  }
}
